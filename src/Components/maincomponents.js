import styled from "@emotion/styled";
import { keyframes } from "@emotion/core";

export const DisplaySpan = styled.span`
  font-weight: 300;
  font-size: 6em;
`;

export const HeaderSpan = styled.span`
  font-weight: 500;
  font-size: 6em;
  line-height: 1.6;
`;

export const TitleOneSpan = styled.span`
  font-weight: 500;
  font-size: 3.9em;
`;

export const TitleTwoSpan = styled.span`
  font-weight: 500;
  font-size: 3em;
`;

export const HeadlineSpan = styled.span`
  font-weight: 400;
  font-size: 2.7em;
`;

export const BodySpan = styled.span`
  font-weight: 400;
  font-size: 1.9em;
`;

export const CaptionSpan = styled.span`
  font-weight: 400;
  font-size: 1.65em;
  line-height: 1.35;
`;

export const WiderSpan = styled.span`
  font-weight: 400;
  letter-spacing: 0.1em;
  font-size: 1.15em;
`;

export const WhiteToRight = keyframes`

`;
